# 大水牛 R7.0 实施执行计划

> **原始需求与背景回溯**：
> - **提出时间**：2026-09-19
> - **业务背景**：`pet-forge` 为动画制作开源工坊，`clawd-on-desk`（`E:\Work2\AI_Work\tool\clawd-on-desk\clawd-on-desk\clawd-on-desk`）为桌面宠物运行时。用户需在 `work_space/R7_dashuiniu/` 下，以参考图片（3D 拟人化霸气猛汉大水牛）为基准，采用 **APNG 路线** 制作高品质桌面宠物动画素材。
> - **用户原始诉求**：“项目下，我想按照APNG 路线方式，按照R7_dashuiniu的图片上的角色生成动画素材，在R7_dashuiniu下，专门用于开源项目E:\Work2\AI_Work\tool\clawd-on-desk\clawd-on-desk\clawd-on-desk使用。你需要看下R1_doraemon，以及开源项目。然后告诉我步骤，并生成R7.0的方案和计划。你需要先给我生成每个动画的提示词，我去用豆包生成适配后，提供给你。需要生动有趣。比如大水牛坐在桌前敲电脑”。
> - **已拍板技术决策**：
>   1. **采用 APNG 路线**：以带 Alpha 透明通道的高清位图动画帧呈现 3D 毛发质感、衣服布料褶皱与生动反差萌动作。
>   2. **Clawd 运行时适配**：关闭 SVG 挂点眼动（`eyeTracking.enabled: false`），利用 Clawd 原生 `<img>` 通道与 `probeApngCycle` 探测循环。
>   3. **布局与尺寸控制**：`viewBox: {"x": 0, "y": 0, "width": 256, "height": 256}`，`visibleHeightRatio: 0.42`，`baselineBottomRatio: 0.05`。
>   4. **漫步朝向与循环闭环**：`roam.apng` 面朝右；循环状态（`working`, `idle`, `roam`, `thinking`, `sleeping`, `error`）首尾帧无缝闭环。
>   5. **交付范围**：7 个核心状态 APNG + `theme.json` + 打包与安装脚本。
> - **对接运行时**：Clawd-on-Desk (`theme.json` Schema v1)
> - **交付目标**：`work_space/R7_dashuiniu/`

---

## 1. 里程碑阶段规划（Milestones & Phases）

```mermaid
flowchart TD
    P1["阶段 1：需求回溯与运行时机制核验 (已完成)"] --> P2["阶段 2：方案制定与豆包提示词工程交付 (当前阶段已就绪)"]
    P2 --> P3["阶段 3：用户使用豆包生成素材并归档"]
    P3 --> P4["阶段 4：自动化抠图去底、APNG 压制与 Theme 构建"]
    P4 --> P5["阶段 5：桌面端安装实测、自动校验与 Git 推送"]
```

---

## 2. 阶段详细任务分解

### 阶段 1：需求回溯与运行时机制核验（已完成）
- [x] **任务 1.1**：核查 `clawd-on-desk` 源码关于多媒体素材的处理逻辑（确认 `settings-i18n.js`、`theme-schema.js`、`animation-cycle.js` 原生支持 APNG/GIF/WebP 硬件加速通道）。
- [x] **任务 1.2**：核查 `R1_doraemon`、`cloudling` 与 `calico` 的 `theme.json` 结构与尺寸契约，确认 APNG 模式下无需挂载 `#eyes-js`（需将 `eyeTracking.enabled` 设为 `false`）。
- [x] **任务 1.3**：创建 `work_space/R7_dashuiniu/` 标准 5 级工作区（`00_reference/`、`01_raw_doubao/`、`02_forge/`、`03_export/`、`theme/assets/`），并完整归档参考图 `dashuiniu_reference.jpg`。
- [x] **任务 1.4**：验证本地 Python 3.12、`Pillow`、`rembg`、`ffmpeg` 自动化处理环境。

### 阶段 2：方案制定与生动豆包提示词交付（当前阶段已就绪）
- [x] **任务 2.1**：编写 `work_space/docs/R7.0_大水牛_设计与技术方案.md`，明确 APNG 技术指标与视口规范。
- [x] **任务 2.2**：编写 `work_space/docs/R7.0_大水牛_实施执行计划.md`。
- [x] **任务 2.3**：编制 7 大核心状态（`working` 反差萌敲电脑、`idle` 沉稳待机、`roam` 巡逻漫步、`thinking` 摸角沉思、`sleeping` 呼噜大睡、`notification` 胜利点赞、`error` 抱头抓狂）的高精准中英文生动提示词（含垫图策略、动作时钟、镜头约束与循环闭环说明）。
- [x] **任务 2.4**：向用户详细汇报实施步骤与提示词，等待用户在豆包完成生图/视频制作。

### 阶段 3：用户侧豆包素材生成与协同协作（等待用户执行）
- [ ] **任务 3.1**：用户打开豆包 AI 视频/动画生成工具，上传 `work_space/R7_dashuiniu/00_reference/dashuiniu_reference.jpg` 作为垫图。
- [ ] **任务 3.2**：按照提示词生成各状态的生动短视频/动图（纯白底无地面阴影，固定机位，无镜头平移缩放）。
- [ ] **任务 3.3**：用户将生成的原始文件（MP4 或 GIF 或已去底透明 APNG）存入 `work_space/R7_dashuiniu/01_raw_doubao/`，通知 Agent 启动后续流水线。

### 阶段 4：素材规范化转换、高精去底、APNG 压制与 Theme 构建（Agent 执行）
- [ ] **任务 4.1**：编写 `work_space/R7_dashuiniu/02_forge/process_all_videos.py` 自动化处理脚本。
- [ ] **任务 4.2**：使用 `ffmpeg` 提取高清序列帧，利用 `rembg` (isnet-anime / u2net) 智能抠图并生成 Alpha 透明通道。
- [ ] **任务 4.3**：对齐帧尺寸至统一居中视口（256×256），检测循环帧平滑度。
- [ ] **任务 4.4**：批量压制生成带透明通道的高品质 `.apng` 文件至 `work_space/R7_dashuiniu/theme/assets/`。
- [ ] **任务 4.5**：生成标准 `work_space/R7_dashuiniu/theme/theme.json`（无 BOM UTF-8）。
- [ ] **任务 4.6**：编写 `work_space/R7_dashuiniu/install-to-clawd.ps1` 与 `package-theme.ps1`。

### 阶段 5：运行时安装实测、自动回归校验与 Git 推送（Agent 执行）
- [ ] **任务 5.1**：执行 `install-to-clawd.ps1`，将大水牛主题一键部署到 Clawd 本地用户主题目录（`%APPDATA%/clawd-on-desk/themes/dashuiniu`）。
- [ ] **任务 5.2**：通过 Clawd 的校验体系验证 `theme.json` 与 APNG 循环周期。
- [ ] **任务 5.3**：严格执行 AGENTS.md Git 纪律，执行 `git add .`、`git commit -m "feat(R7): 交付大水牛 R7.0 方案计划、工程骨架与豆包提示词工程"` 并推送到远程仓库 `https://github.com/RyanChenJJH/pet-desk.git`。

---

## 3. 质量门禁与成功判据

1. **APNG 视觉品质**：
   - 背景 100% 透明，无白色毛边、杂色或环境光斑残留；
   - 待机、工作敲电脑、思考、漫步与睡眠状态无缝平滑循环，无突兀跳跃与抽帧感。
2. **Clawd-on-desk 兼容性**：
   - `theme.json` 为无 BOM UTF-8，`JSON.parse` 无异常；
   - 桌面显示比例协调（`visibleHeightRatio: 0.42`），居中贴合底边，不遮挡主工作屏幕；
   - 漫步巡逻方向正确（面朝右，向左走时系统自动水平翻转）；
   - 各项状态（尤其敲电脑、打瞌睡、沉思）切换自然流畅。
3. **版本库规范**：
   - 方案与计划完整归档于 `work_space/docs/`；
   - 远程仓库 `https://github.com/RyanChenJJH/pet-desk.git` 成功同步最新交付物。
